# Tenant namespace — the blast-radius boundary for team-b.
# The platform team (this repo) owns the namespace; the tenant only ever
# operates *inside* it via the impersonated ServiceAccount (see rbac.yaml).
apiVersion: v1
kind: Namespace
metadata:
  name: team-b
  labels:
    toolkit.fluxcd.io/tenant: team-b
