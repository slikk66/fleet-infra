apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Aggregates every tenant's guardrail bundle for the hardway cluster.
# Pointed at by the cluster-level Flux Kustomization clusters/hardway/tenants.yaml.
resources:
  - team-a
  - team-b
