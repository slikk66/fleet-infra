apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Platform networking bundle for hardway: the shared Gateway + its namespace.
# Pointed at by the cluster-level Flux Kustomization clusters/hardway/gateway.yaml.
resources:
  - namespace.yaml
  - gateway.yaml
