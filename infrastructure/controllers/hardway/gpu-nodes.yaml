# Patches pre-existing Node objects to add the fake-gpu-operator
# discovery label. The prune-disabled annotation is REQUIRED: Nodes are
# co-owned with kubelet; deleting this file from git must NOT cause Flux
# to delete the Node API objects (which would evict all pods on them).
---
apiVersion: v1
kind: Node
metadata:
  name: worker-1
  labels:
    run.ai/simulated-gpu-node-pool: default
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
---
apiVersion: v1
kind: Node
metadata:
  name: worker-2
  labels:
    # MIG demo: worker-2 joins the 'mig' nodePool → its device-plugin advertises
    # 1 whole GPU + the MIG slices (nvidia.com/mig-3g.40gb, mig-1g.10gb).
    run.ai/simulated-gpu-node-pool: mig
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
