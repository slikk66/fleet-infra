apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# metrics-server base (HelmRepository + installer SA + HelmRelease), all in
# kube-system. Referenced by the hardway overlay only (k3s has its own).
resources:
  - helmrepository.yaml
  - rbac.yaml
  - helmrelease.yaml
