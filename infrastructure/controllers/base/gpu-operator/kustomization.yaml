apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared base for the fake-gpu-operator HelmRelease and its namespace.
# Per-cluster overlays supply a gpu-operator-values ConfigMap that the HR
# references via valuesFrom.
resources:
  - namespace.yaml
  - helmrelease.yaml
